### 🚀 Added
- Settings to automatically calculate reward amount from number of datasets

### ✏️ Changed

- Fixed a bug causing timestamps time-related settings having a non-zero second and millisecond part (potentially causing issues in edge cases)
- Fixed a rendering bug, where time input fields would be rendered as --:-- in some locales